/**
 * Converts milliseconds to minutes.
 * @param {number} milliseconds - The time in milliseconds to convert.
 * @returns {number} - The converted time in minutes.
 */
export const millisecondsToMinutes = milliseconds => milliseconds / 1000 / 60;
