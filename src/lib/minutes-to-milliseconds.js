/**
 * Converts minutes to milliseconds.
 * @param {number} minutes - The time in minutes to convert.
 * @returns {number} - The converted time in milliseconds.
 */
export const minutesToMilliseconds = minutes => minutes * 60 * 1000;
